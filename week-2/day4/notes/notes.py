# Update conda:
# conda update conda

# Crear nuevo invironment
# conda create --name environment_name python=3.6

# Chequear que pip este!
# Proceed (y/n)? y

# Para activar el environment:
# activate environment_name (windows)
# conda activate environment_name
# source activate environment_name

# Para desactivarlo
# deactivate (windows)
# conda desactivate environment_name
# source desactivate environment_name

# Remove environment
# conda remove --name environment_name --all

# Chequear environmets activos:
# conda info --envs

# pip freeze (packages install in the current environment)
# pip freeze --all
# pip install django

# Crea environment con los packages especificados en el archivo requirements
# conda env create -f requirements.yml
# activate my_project (el nombre que se coloca en el archivo de requirements, sera el nombre del env)

# una vez creado el environment, en PyCharm --> settings
# --> project environment (y buscarlo en Anaconda3 --> envs --> environment_name --> python)

# nano ~/.zshrc (MAC)


